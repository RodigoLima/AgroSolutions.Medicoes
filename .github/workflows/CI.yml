name: CI

on:
  pull_request:
    branches: ["*"]
  push:
    branches: ["main"]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "8.0.x"
          cache: false

      - name: Restore
        run: dotnet restore AgroSolutions.Medicoes.sln

      - name: Build
        run: dotnet build AgroSolutions.Medicoes.sln --no-restore --configuration Release

      - name: Run Tests
        run: dotnet test AgroSolutions.Medicoes.sln --configuration Release --no-build --verbosity minimal
